# Third-party Dependencies

This repository uses CMake to discover or fetch the following third-party libraries:

- Eigen
- nlohmann/json
- Catch2

The current scaffold downloads them automatically when `SDF_CONTACT_LAB_FETCH_DEPS=ON`.
